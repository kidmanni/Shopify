{% assign current_product = product.selected_or_first_available %}
{% assign product_image = current_product.featured_image | default: product.featured_image %}

<div class="grid grid-cols-2 mt-10 ml-20">
  <div class="flex h-screen">
    <div class="overflow-auto h-3/4">
      {%- comment -%}Need find nice js scroller (Ill do it, I wolf you){%- endcomment -%}
      {% for image in product.images %}
      <img class="py-2" src="{{ image.src | img_url: 'medium' }} " alt="{{ image.alt }} ">
      {% endfor %}
      {%- comment -%}change image on click to main{%- endcomment -%}
    </div>
    <div class="">
      <img src="{{ product_image | product_img_url: '750x750' }} " alt="{{ product_image.alt }}" id="ProductMainImage">
    </div>
  </div>
  <div class="flex flex-col">
    <h1>{{ product.title }} </h1>
    <p>{{ current_product.price | money }} </p>

    {% form 'product', product, class: "product-form", id:"AddToCartForm" %}
    <div class="mt-3">
      <select name="id" id="productSelect" class="form-selector">
        {% for variant in product.variants %}
        {% if variant.available %}
        <option value="{{ variant.id }}">
          {{ variant.title }}
        </option>
        {% else %}
        <option value="{{ variant.id }}" disabled="disabled">
          {{ variant.title }}
        </option>

        {% endif %}

        {% endfor %}
      </select>
    </div>
    {% endform %}
  </div>
</div>
{%- comment -%}
  {% section 'product-template' %}
  Recomendations by clients actions
{%- endcomment -%}
{% section 'product-recommendations' %}

{%- comment -%}
  Recomendations by tag (controled by owner of the store)
  
{%- endcomment -%}
{% section 'related-products-by-tag' %}

{%- comment -%}
API CALL EXAMPLE 
- Recomendations endpoint: https://alant-kids.myshopify.com/recommendations/products.json?product_id=6885168316597&limit=10 
- All products endpoint: https://alant-kids.myshopify.com/products.json
- Product by Id: https://alant-kids.myshopify.com/admin/api/2021-04/products/6885168054453.json

{%- endcomment -%}